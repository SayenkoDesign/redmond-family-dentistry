{# ready to schedule row in home page #}
<section class="call-to-action">
    <div class="row">
        <div class="heading">{{ SP.ACF.get_sub_field('heading') }}</div>
        <div class="action"><a href="{{ SP.WordPress.URL(SP.ACF.get_sub_field('button_url')) }}" class="large primary button">{{ SP.ACF.get_sub_field('button_text') }}</a></div>
    </div>
</section>